import Link from "next/link";

const Header = () => {
  return (
    <header>
      <nav className="p-2">
        <Link href="/">能力開発navi</Link>
      </nav>
    </header>
  );
};

export default Header;
